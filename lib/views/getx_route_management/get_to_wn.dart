import 'package:flutter/material.dart';

class GetToWM extends StatelessWidget {
  const GetToWM({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Get TO')),
    );
  }
}
