import 'package:flutter/material.dart';

class GetOffWM extends StatelessWidget {
  const GetOffWM({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Get OFF')),
    );
  }
}
